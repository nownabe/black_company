require 'spec_helper'

describe BlackCompany do
  it 'has a version number' do
    expect(BlackCompany::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
